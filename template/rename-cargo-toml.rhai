// To avoid Rust Analyzer picking up this template as a normal Rust project,
// we use a different name for `Cargo.toml` within the template. Rename it
// when creating the project.
file::rename("Cargo-template.toml", "Cargo.toml");
